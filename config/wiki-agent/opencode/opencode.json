{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": "deny",
    "webfetch": "deny",
    "external_directory": "deny"
  },
  "agent": {
    "wiki-skill": {
      "description": "Runs one wiki skill over the vault. Writes markdown, nothing else.",
      "mode": "primary",
      "permission": {
        "edit": "allow"
      }
    },
    "wiki-ask": {
      "description": "Answers one question from the vault. Reads only.",
      "mode": "primary",
      "permission": {
        "edit": "deny"
      }
    }
  }
}
